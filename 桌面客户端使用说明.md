# 桌面客户端使用说明

**版本：** 2.0.0  
**基于：** PyQt5  
**适用场景：** 离线演示、无浏览器环境、独立部署

---

## 简介

桌面客户端是本系统的独立桌面应用版本，基于 PyQt5 开发，提供与 Web UI 相同的核心功能，但无需浏览器即可运行。

**核心优势：**

- **独立运行**：不依赖浏览器，适合离线环境
- **原生体验**：响应速度快，界面流畅
- **便于演示**：单窗口应用，适合评委现场演示
- **易于打包**：可使用 PyInstaller 打包成 exe，一键分发

---

## 启动方式

### 命令行启动

在项目根目录执行：

```bash
python desktop_app.py
```

**启动成功标志**：弹出应用窗口，标题栏显示 **"Scapy Sentinel · 网络异常流量检测器"**

### 权限要求

**实时监控**需要管理员权限：

- **Windows**：右键点击 PowerShell/CMD，选择"以管理员身份运行"，然后执行启动命令
- **Linux/macOS**：使用 `sudo python desktop_app.py`

**PCAP 文件分析**无需特殊权限。

---

## 界面布局

### 主窗口结构

```
┌─────────────────────────────────────────────────────────────┐
│  Scapy Sentinel · 网络异常流量检测器                          │
├──────────┬──────────────────────────────────────────────────┤
│          │                                                  │
│  侧边栏   │                 主视图区域                         │
│          │                                                  │
│  ■ 概览  │  根据侧边栏选择，显示不同的功能页面：                │
│  ◉ 实时  │  - 概览：统计卡片                                  │
│  ▤ PCAP  │  - 实时监控：网卡选择、流量图表、告警列表           │
│  ⚑ 告警  │  - PCAP 分析：文件上传、分析结果                   │
│          │  - 告警中心：历史告警列表                          │
│          │                                                  │
├──────────┴──────────────────────────────────────────────────┤
│  状态栏：就绪 / 正在抓包 / 正在分析...                         │
└─────────────────────────────────────────────────────────────┘
```

### 侧边栏导航

点击侧边栏图标切换页面：

- **■ 概览**：系统状态总览，实时统计卡片
- **◉ 实时监控**：网卡选择、实时抓包、流量图表
- **▤ PCAP 分析**：文件上传、离线分析、报告导出
- **⚑ 告警中心**：历史告警列表、筛选与导出

---

## 功能详解

### 功能一：概览页

**作用**：快速了解系统当前状态。

**显示内容**：

- **统计卡片**（4 个）：
  - 总包数：系统启动以来处理的数据包总数
  - 告警数：检测到的异常流量总数
  - 协议分布：TCP/UDP/ICMP/其他的占比
  - 检测引擎状态：规则/ML/Transformer 是否就绪

- **实时刷新**：每 5 秒自动更新数据

**适用场景**：
- 评委演示时的首页
- 快速检查系统运行状态

---

### 功能二：实时监控

**作用**：监控当前网络流量，发现正在进行的攻击。

**操作步骤**：

1. **选择网络接口**：
   - 下拉菜单显示所有可用网卡
   - 格式：`接口名称 (IP地址)`，例如 `以太网 (192.168.1.100)`
   - 不确定选哪个？选择当前正在使用的网络接口（有 IP 地址的那个）

2. **（可选）设置 BPF 过滤器**：
   - 默认：`tcp or udp`（只抓取 TCP 和 UDP 流量）
   - 常用过滤器：
     - `tcp port 80 or tcp port 443`：只抓取 HTTP/HTTPS 流量
     - `icmp`：只抓取 ICMP（ping）流量
     - `host 192.168.1.100`：只抓取与特定 IP 相关的流量

3. **点击"开始监控"**：
   - 按钮变为"停止监控"
   - 状态栏显示"正在抓包..."
   - 如提示权限不足，请以管理员身份重新启动

4. **查看实时数据**：
   - **流量趋势图**（上方）：
     - 横轴：时间（最近 60 秒）
     - 纵轴：包速率（pps）和字节速率（Bps）
     - 双 Y 轴显示，左侧为包速率，右侧为字节速率
   - **协议分布饼图**（中间）：
     - TCP、UDP、ICMP、其他协议的占比
     - 鼠标悬停显示具体数量
   - **告警列表**（下方）：
     - 按时间倒序显示
     - 红色：高危告警（SYN 洪水、DDoS）
     - 橙色：中危告警（端口扫描、UDP 洪水）
     - 黄色：低危告警（异常流量模式）

5. **停止监控**：点击"停止监控"按钮

**关键指标解读**：

- **包速率（pps）**：每秒接收的数据包数量
  - 正常值：< 1000 pps
  - 异常值：> 5000 pps（可能是洪水攻击）
- **字节速率（Bps）**：每秒接收的字节数
  - 取决于网络带宽，100Mbps 网络约 12.5MB/s

---

### 功能三：PCAP 分析

**作用**：对已保存的网络数据包文件进行离线分析。

**操作步骤**：

1. **选择文件**：
   - 点击"浏览"按钮
   - 选择 `.pcap` 或 `.pcapng` 文件
   - 文件路径显示在输入框中

2. **点击"开始分析"**：
   - 进度条显示解析进度
   - 状态栏显示"正在分析..."
   - 分析时间：1MB 约 2-5 秒，100MB 约 3-8 分钟

3. **查看分析结果**：
   - **统计摘要**（上方）：
     - 总包数、总字节数
     - 时间跨度（首包到末包的时间）
     - 平均包速率、平均字节速率
   - **协议分布**（中间）：
     - TCP/UDP/ICMP/其他的数量与占比
     - 饼图可视化
   - **告警列表**（下方）：
     - 表格形式，列：时间、源 IP、目的 IP、攻击类型、严重程度、置信度
     - 双击行可查看详细信息

4. **导出报告**：
   - 点击"生成分析报告"按钮
   - 选择保存位置（默认：`data/pcap_analysis_report.md`）
   - 报告格式：Markdown，可用 Typora 等工具打开

**PCAP 文件获取方式**：

- **Wireshark 抓包**：Capture → Start，停止后 File → Save As
- **tcpdump 命令**：`sudo tcpdump -i eth0 -w capture.pcap`
- **本系统实时监控**：监控过程中自动保存到 `data/` 目录

---

### 功能四：告警中心

**作用**：查看历史告警记录，筛选与导出。

**操作步骤**：

1. **查看告警列表**：
   - 表格显示所有历史告警
   - 列：时间、源 IP、目的 IP、攻击类型、严重程度、检测器

2. **刷新列表**：
   - 点击"刷新"按钮，从数据库重新加载

3. **筛选告警**（可选）：
   - 按时间范围筛选：选择开始/结束日期
   - 按 IP 筛选：输入源 IP 或目的 IP
   - 按攻击类型筛选：下拉选择（SYN 洪水、端口扫描等）

4. **导出数据**：
   - 点击"导出 CSV"按钮
   - 选择保存位置
   - 可用 Excel 打开，进一步分析

---

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+Q` | 退出应用 |
| `F5` | 刷新当前页面 |
| `Ctrl+1` | 切换到概览页 |
| `Ctrl+2` | 切换到实时监控页 |
| `Ctrl+3` | 切换到 PCAP 分析页 |
| `Ctrl+4` | 切换到告警中心页 |

---

## 与 Web UI 的区别

| 特性 | 桌面客户端 | Dash Web UI |
|------|-----------|-------------|
| **启动方式** | `python desktop_app.py` | `python DashWeb.py` + 浏览器 |
| **运行环境** | 独立窗口 | 浏览器 |
| **离线使用** | ✅ 支持 | ❌ 需要浏览器 |
| **响应速度** | 快（原生 Qt） | 中等（HTTP 请求） |
| **功能完整性** | 核心功能 | 全部功能 |
| **适用场景** | 离线演示、独立部署 | 日常使用、远程访问 |
| **打包分发** | 可打包成 exe | 需要 Python 环境 |

---

## 常见问题

### Q1: 启动时提示"找不到模块 PyQt5"

**原因**：未安装 PyQt5 依赖。

**解决方案**：
```bash
pip install PyQt5
```

---

### Q2: 实时监控时程序卡顿

**原因**：流量过大，UI 线程被阻塞。

**解决方案**：
1. 使用 BPF 过滤器，减少抓包量
2. 降低刷新频率：编辑 `src/desktop/workers.py`，修改 `sleep(0.1)` 为 `sleep(0.5)`

---

### Q3: 如何打包成 exe 文件？

**步骤**：

1. 安装 PyInstaller：
   ```bash
   pip install pyinstaller
   ```

2. 打包：
   ```bash
   pyinstaller --onefile --windowed --name "网络异常流量检测器" desktop_app.py
   ```

3. 生成的 exe 位于 `dist/` 目录

**注意**：
- 打包后的 exe 体积较大（约 100-200MB），因为包含了 Python 解释器和所有依赖
- 需要将 `models/` 目录和 `data/` 目录一起分发

---

### Q4: 如何自定义主题颜色？

**步骤**：

编辑 `src/desktop/theme.py`，修改 `Palette` 类中的颜色值：

```python
class Palette:
    primary = "#3b82f6"      # 主色调（蓝色）
    secondary = "#8b5cf6"    # 次要色（紫色）
    success = "#10b981"      # 成功色（绿色）
    warning = "#f59e0b"      # 警告色（橙色）
    danger = "#ef4444"       # 危险色（红色）
    background = "#1e293b"   # 背景色（深灰）
    surface = "#334155"      # 卡片背景（中灰）
    text = "#f1f5f9"         # 文字色（浅灰）
```

重启应用即可生效。

---

## 系统要求

- **操作系统**：Windows 10/11、macOS 10.15+、Linux（Ubuntu 20.04+）
- **Python 版本**：3.10+
- **内存**：至少 4GB（8GB 更佳）
- **CPU**：多核处理器（推荐 4 核以上）
- **显示器分辨率**：至少 1280x720（推荐 1920x1080）

---

## 版本信息

- **版本**：2.0.0
- **发布日期**：2026-04-08
- **开发者**：南京邮电大学 B22041217 辛晗宇
- **基于框架**：PyQt5 5.15+

---

## 下一步

- **启动指南**：`启动指南.md` - 详细安装与启动说明
- **用户手册**：`用户手册.md` - 功能使用手册
- **技术文档**：`README.md` - 系统架构与技术栈

---

**祝您使用愉快！**
