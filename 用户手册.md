# 网络异常流量检测系统 - 用户手册

**版本：** 2.0.0  
**作者：** 南京邮电大学 B22041217 辛晗宇  
**适用对象：** 网络管理员、安全运维人员、评委老师

---

## 系统简介

本系统是一个基于混合检测引擎的网络异常流量实时检测平台，能够：

- **实时监控**网络流量，自动识别 SYN 洪水、DDoS、端口扫描等 10+ 种攻击
- **离线分析** PCAP 文件，生成详细的流量分析报告
- **三层检测**：规则匹配（快速）+ 机器学习（泛化）+ Transformer（深度）
- **多种界面**：Dash Web UI（主推）、Streamlit UI（备用）、PyQt5 桌面客户端

---

## 界面导航

### Dash Web UI（推荐）

访问 `http://localhost:8050` 后，您将看到四个主要页面：

| 页面 | 功能 | 适用场景 |
|------|------|----------|
| **概览** | 系统状态总览、统计卡片 | 快速了解当前网络状况 |
| **实时监控** | 选择网卡、实时抓包、流量图表 | 日常监控、发现正在进行的攻击 |
| **PCAP 分析** | 上传文件、全量分析、报告导出 | 事后分析、故障排查、学习研究 |
| **历史记录** | 告警查询、流量统计、数据导出 | 查看历史告警、生成统计报表 |

### Streamlit UI（备用）

访问 `http://localhost:8501`，单页应用，左侧边栏切换模式：

- **实时抓包模式**：选择网卡 → 开始抓包 → 查看实时数据
- **PCAP 文件模式**：上传文件 → 等待分析 → 查看结果

### PyQt5 桌面客户端

独立桌面应用，侧边栏导航：

- **概览**：实时统计卡片（总包数、告警数、协议分布）
- **实时监控**：网卡选择、流量图表、告警列表
- **PCAP 分析**：文件上传、分析进度、结果展示
- **告警中心**：历史告警列表、筛选与导出

---

## 核心操作流程

### 操作一：实时监控网络流量

**目标**：监控当前网络，发现正在进行的攻击。

**步骤**：

1. **启动系统**（以 Dash 为例）：
   ```bash
   python DashWeb.py
   ```
   访问 `http://localhost:8050`

2. **进入实时监控页面**：点击左侧导航栏的"实时监控"

3. **选择网络接口**：
   - 下拉菜单中选择您的网卡（例如：`以太网`、`WLAN`、`eth0`）
   - 不确定选哪个？选择当前正在使用的网络接口（有 IP 地址的那个）

4. **点击"开始监控"**：
   - 系统开始抓包（需要管理员权限）
   - 如提示权限不足，请以管理员身份重新启动

5. **查看实时数据**：
   - **流量趋势图**：横轴为时间，纵轴为包速率/字节速率
   - **协议分布饼图**：TCP、UDP、ICMP、其他协议的占比
   - **告警列表**：按时间倒序显示，红色为高危告警

6. **停止监控**：点击"停止"按钮

**关键指标解读**：

- **包速率（pps）**：每秒接收的数据包数量，正常值 < 1000，异常值 > 5000
- **字节速率（Bps）**：每秒接收的字节数，正常值取决于网络带宽
- **告警严重程度**：
  - **高危（红色）**：SYN 洪水、DDoS、ARP 欺骗 - 需立即处理
  - **中危（橙色）**：端口扫描、UDP 洪水 - 需关注
  - **低危（黄色）**：异常流量模式 - 可记录

---

### 操作二：分析 PCAP 文件

**目标**：对已保存的网络数据包文件进行离线分析。

**步骤**：

1. **进入 PCAP 分析页面**：点击左侧导航栏的"PCAP 分析"

2. **上传文件**：
   - 点击上传区域或拖放文件
   - 支持格式：`.pcap`、`.pcapng`
   - 文件大小：建议 < 500MB（大文件分析较慢）

3. **等待分析**：
   - 进度条显示解析进度
   - 分析时间：1MB 约 2-5 秒，100MB 约 3-8 分钟

4. **查看分析结果**：
   - **流量统计摘要**：总包数、总字节数、时间跨度、平均速率
   - **协议分布**：TCP/UDP/ICMP/其他的数量与占比
   - **告警列表**：检测到的攻击类型、源/目的 IP、置信度、触发规则

5. **导出报告**：
   - 点击"生成报告"按钮
   - 下载 Markdown 格式的分析报告（可用 Typora 等工具打开）
   - 报告包含：流量统计、协议分布、告警详情、检测引擎信息

**PCAP 文件获取方式**：

- **Wireshark 抓包**：Capture → Start，停止后 File → Save As
- **tcpdump 命令**：`sudo tcpdump -i eth0 -w capture.pcap`
- **本系统实时监控**：监控过程中自动保存到 `data/` 目录

---

### 操作三：查看历史告警

**目标**：查询过去一段时间的告警记录，生成统计报表。

**步骤**：

1. **进入历史记录页面**：点击左侧导航栏的"历史记录"

2. **筛选条件**：
   - **时间范围**：选择日期区间（例如：最近 7 天）
   - **IP 地址**：输入源 IP 或目的 IP（可选）
   - **攻击类型**：下拉选择（例如：SYN 洪水、端口扫描）

3. **查看结果**：
   - **告警列表**：表格形式，显示时间、IP、攻击类型、严重程度
   - **时间序列图**：横轴为时间，纵轴为告警数量
   - **攻击类型分布**：饼图显示各类攻击的占比

4. **导出数据**：
   - 点击"导出 CSV"按钮，下载告警数据
   - 可用 Excel 打开，进一步分析

---

## 理解检测结果

### 告警字段说明

每条告警包含以下信息：

| 字段 | 说明 | 示例 |
|------|------|------|
| **时间戳** | 告警触发时间 | 2026-04-08 14:32:15 |
| **源 IP** | 攻击来源 IP 地址 | 192.168.1.100 |
| **目的 IP** | 攻击目标 IP 地址 | 10.0.0.5 |
| **攻击类型** | 检测到的攻击类别 | syn_flood |
| **严重程度** | 危害等级 | high / medium / low |
| **置信度** | 检测器的确信程度（0-1） | 0.95 |
| **检测器** | 触发告警的检测器 | rule / ml / transformer |
| **详情** | 触发规则或模型输出 | {"rule": "SYN 洪水", "threshold": 50, "actual": 127} |

### 攻击类型详解

#### 1. SYN 洪水（syn_flood）

**原理**：攻击者发送大量 TCP SYN 包，不完成三次握手，耗尽服务器连接资源。

**检测依据**：5 秒窗口内 SYN 包数量 > 50

**应对措施**：
- 启用 SYN Cookie 防护
- 限制单 IP 的 SYN 速率
- 使用防火墙拦截攻击源 IP

#### 2. DDoS 攻击（ddos）

**原理**：分布式拒绝服务，多台机器同时攻击，让目标服务瘫痪。

**检测依据**：
- 包速率 > 5000 pps
- 字节速率 > 5MB/s
- 来自多个不同源 IP

**应对措施**：
- 联系 ISP 启用上游过滤
- 使用 CDN 分散流量
- 启用 DDoS 防护服务（如 Cloudflare）

#### 3. 端口扫描（port_scan）

**原理**：攻击者探测目标主机的开放端口，寻找漏洞。

**检测依据**：5 秒内访问不同端口数 > 20

**应对措施**：
- 关闭不必要的端口
- 启用防火墙，只开放必需端口
- 记录扫描源 IP，加入黑名单

#### 4. UDP/ICMP 洪水（udp_flood / icmp_flood）

**原理**：发送大量 UDP 或 ICMP 包，占用网络带宽。

**检测依据**：
- UDP 洪水：5 秒内 UDP 包数 > 3000
- ICMP 洪水：5 秒内 ICMP 包数 > 2000

**应对措施**：
- 限制 ICMP 速率（ping 限速）
- 过滤非必要的 UDP 流量

#### 5. ARP 欺骗（arp_spoof）

**原理**：攻击者伪造 ARP 响应，劫持局域网流量。

**检测依据**：同一 IP 对应多个不同 MAC 地址

**应对措施**：
- 启用静态 ARP 绑定
- 使用 ARP 防火墙
- 隔离可疑主机

---

## 检测引擎说明

本系统采用**三层混合检测**，不同检测器各有优势：

### 1. 规则匹配器（RuleMatcher）

**工作原理**：基于阈值规则，快速匹配已知攻击模式。

**优势**：
- 速度快（< 1ms）
- 零误报（规则精确）
- 适合已知攻击

**局限**：
- 无法检测未知攻击
- 需要人工维护规则库

**触发示例**：
```json
{
  "detector": "rule",
  "attack_type": "syn_flood",
  "details": {
    "rule": "SYN 洪水检测",
    "threshold": 50,
    "actual": 127,
    "confidence": 1.0
  }
}
```

### 2. 机器学习检测器（MLDetector）

**工作原理**：基于 sklearn 随机森林模型，学习正常流量模式，识别异常。

**优势**：
- 泛化能力强
- 可检测未知攻击
- 自动学习特征

**局限**：
- 需要训练数据
- 可能有误报

**触发示例**：
```json
{
  "detector": "ml",
  "attack_type": "anomaly",
  "details": {
    "model": "RandomForest",
    "confidence": 0.87,
    "features": {
      "packet_rate": 4523,
      "byte_rate": 3821045,
      "avg_pkt_len": 845
    }
  }
}
```

### 3. Transformer 检测器（TransformerDetector）

**工作原理**：基于深度学习的自注意力机制，捕捉流量时序依赖。

**优势**：
- 检测复杂攻击序列
- 捕捉长期依赖
- 适合 APT 检测

**局限**：
- 计算开销大（50ms）
- 需要 GPU 加速（可选）

**触发示例**：
```json
{
  "detector": "transformer",
  "attack_type": "ddos",
  "details": {
    "model": "Transformer-8L",
    "confidence": 0.94,
    "attention_weights": [0.12, 0.34, 0.28, ...]
  }
}
```

---

## 常见问题

### Q1: 为什么实时监控看不到流量？

**可能原因**：
1. 选择了错误的网络接口
2. 接口未激活（没有 IP 地址）
3. 没有实际流量通过该接口

**解决方案**：
1. 切换到其他网络接口
2. 确认接口状态：`ipconfig`（Windows）或 `ifconfig`（Linux/macOS）
3. 生成测试流量：打开浏览器访问网站，或 `ping 8.8.8.8`

---

### Q2: 告警太多，如何判断哪些是真实攻击？

**判断标准**：

1. **置信度**：> 0.9 的告警优先处理
2. **检测器**：`rule` 检测器的告警几乎无误报
3. **严重程度**：`high` 级别的告警需立即处理
4. **重复性**：同一源 IP 短时间内多次触发 → 真实攻击可能性大

**降低误报**：

- 调整规则阈值：编辑 `src/core/detection_engine.py`
- 白名单机制：将可信 IP 加入白名单
- 关闭 ML 检测器：只使用规则匹配（零误报）

---

### Q3: 如何调整检测灵敏度？

**提高灵敏度**（检测更多异常，但可能增加误报）：

编辑 `src/core/detection_engine.py`，降低阈值：

```python
"syn_flood": {
    "threshold": 30,  # 原值 50，降低到 30
    "description": "SYN 洪水攻击检测",
    "weight": 0.9
}
```

**降低灵敏度**（减少误报，但可能漏检）：

提高阈值：

```python
"syn_flood": {
    "threshold": 100,  # 原值 50，提高到 100
    "description": "SYN 洪水攻击检测",
    "weight": 0.9
}
```

---

### Q4: PCAP 文件分析很慢怎么办？

**优化方案**：

1. **拆分大文件**：使用 Wireshark 的 `editcap` 工具拆分
   ```bash
   editcap -c 100000 large.pcap split.pcap
   ```

2. **关闭 Transformer**：编辑 `src/core/detection_engine.py`，注释掉 Transformer 检测器

3. **使用 BPF 过滤**：只分析关心的流量
   ```python
   source = PcapSource("file.pcap", bpf_filter="tcp port 80")
   ```

---

### Q5: 如何导出分析报告？

**Dash Web UI**：
1. 进入 PCAP 分析页面
2. 上传文件并等待分析完成
3. 点击"生成报告"按钮
4. 下载 Markdown 格式报告

**Streamlit UI**：
1. 分析完成后，页面底部显示"下载报告"按钮
2. 点击下载

**桌面客户端**：
1. PCAP 分析完成后，点击"导出报告"按钮
2. 选择保存位置

**报告内容**：
- 流量统计摘要
- 协议分布
- 告警列表（按严重程度排序）
- 检测引擎信息
- 时间戳与版本信息

---

## 实用技巧

### 技巧 1：自定义检测规则

如果您想检测特定的攻击模式，可以添加自定义规则：

编辑 `src/core/detection_engine.py`：

```python
"custom_attack": {
    "threshold": 100,
    "description": "自定义攻击检测",
    "weight": 0.85
}
```

然后在 `RuleMatcher.match()` 方法中添加检测逻辑。

---

### 技巧 2：定时自动监控

使用系统定时任务，每天自动监控并生成报告：

**Windows（任务计划程序）**：
1. 打开"任务计划程序"
2. 创建基本任务 → 设置触发器（每天 8:00）
3. 操作：启动程序 → `python DashWeb.py`

**Linux（cron）**：
```bash
crontab -e
# 添加：每天 8:00 启动
0 8 * * * cd /path/to/project && python DashWeb.py
```

---

### 技巧 3：集成到现有监控系统

本系统支持通过 API 集成到 Grafana、Prometheus 等监控平台：

1. 启动 Dash Web UI
2. 访问 `/api/alerts` 获取告警 JSON
3. 配置 Grafana 数据源指向该 API

---

## 下一步

- **启动指南**：`启动指南.md` - 详细安装与启动说明
- **桌面客户端说明**：`桌面客户端使用说明.md` - PyQt5 客户端专项指南
- **技术文档**：`README.md` - 系统架构与技术栈
- **模型训练**：`docs/ml_model_training_design.md` - 训练自己的检测模型

---

**祝您使用愉快！如有问题，请查阅启动指南或联系作者。**
