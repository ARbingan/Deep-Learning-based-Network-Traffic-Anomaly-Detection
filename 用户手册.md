# 网络异常流量检测器 - 用户手册

**让网络监控变得简单！**

**版本：** 1.0.0  
**作者：** 南京邮电大学 B22041217 辛晗宇  
**适用对象：** 零基础用户、网络管理员、安全运维人员

---

## 📖 前言

您好！欢迎使用网络异常流量检测器。

本手册专为**零基础用户**编写，我们会用最简单的语言，手把手教您如何使用本系统。即使您不懂网络技术、不会编程，也能轻松上手！

### 本系统能帮您做什么？

- 🔍 **发现网络攻击**：自动识别黑客攻击、病毒传播等异常行为
- 📊 **监控网络流量**：实时查看网络使用情况，了解谁在占用带宽
- 🚨 **及时告警**：发现异常立即提醒，防止损失扩大
- 📈 **分析历史数据**：查看过去一段时间的网络状况

### 适合谁使用？

- 企业网络管理员
- 学校机房管理员
- 网吧管理员
- 对网络安全感兴趣的新手
- 需要监控网络的学生或研究人员

---

## 📋 目录

1. [系统简介](#1-系统简介)
2. [安装准备](#2-安装准备)
3. [快速开始](#3-快速开始) - **从这里开始！**
4. [界面详解](#4-界面详解)
5. [常用操作](#5-常用操作)
6. [理解检测结果](#6-理解检测结果)
7. [常见问题](#7-常见问题)
8. [实用技巧](#8-实用技巧)

---

## 1. 系统简介

### 1.1 什么是网络异常流量检测？

想象一下，您的网络就像一条公路：
- **正常流量**：汽车有序行驶，遵守交通规则
- **异常流量**：有人恶意堵路、超速行驶、或者反复探查道路

本系统就是您的"交通监控摄像头"，自动发现网络中的异常行为并发出警报。

### 1.2 系统能检测什么？

| 攻击类型 | 通俗解释 | 危害程度 |
|---------|---------|---------|
| **SYN 洪水** | 恶意发送大量连接请求，耗尽服务器资源 | ⭐⭐⭐⭐⭐ |
| **DDoS 攻击** | 多台机器同时攻击，让网站/服务瘫痪 | ⭐⭐⭐⭐⭐ |
| **端口扫描** | 黑客在探测您的网络漏洞 | ⭐⭐⭐⭐ |
| **数据包洪水** | 发送海量数据包，占用网络带宽 | ⭐⭐⭐⭐ |
| **UDP/ICMP 洪水** | 类似洪水攻击，使用特定协议 | ⭐⭐⭐⭐ |

### 1.3 两种使用模式

#### 模式一：实时监控（推荐新手使用）
- **作用**：像看监控一样，实时观察网络流量
- **适用场景**：日常监控、发现正在进行的攻击
- **操作难度**：⭐ 非常简单

#### 模式二：文件分析
- **作用**：分析已经保存的网络数据文件
- **适用场景**：事后分析、故障排查、学习研究
- **操作难度**：⭐⭐ 简单

---

## 2. 安装准备

### ⚠️ 重要提示

**安装只需一次！** 安装完成后，以后使用只需"双击启动"即可。

### 2.1 检查您的电脑

#### 需要什么？
- **操作系统**：Windows 10/11（推荐）、Linux 或 macOS
- **内存**：至少 4GB（8GB 更佳）
- **硬盘空间**：至少 500MB 空闲空间
- **网络**：需要联网下载安装包

#### 需要什么权限？
- **Windows 用户**：需要**管理员权限**（右键选择"以管理员身份运行"）
- **Linux/Mac 用户**：需要输入管理员密码（sudo）

### 2.2 安装步骤（手把手教程）

#### 第1步：下载并解压项目

1. 下载项目文件到您的电脑，例如保存到 `D:\network-detector\`
2. 确保目录结构如下：
   ```
   D:\network-detector\
   ├── requirements.txt
   ├── src\
   │   ├── streamlit_app.py
   │   └── core\
   └── data\
   ```

#### 第2步：安装 Python（如果还没有）

1. 打开浏览器，访问 https://www.python.org/downloads/
2. 下载 **Python 3.10 或更高版本**
3. 安装时**务必勾选** "Add Python to PATH"
4. 安装完成后，打开命令提示符（按 Win+R，输入 `cmd`），输入：
   ```bash
   python --version
   ```
   如果显示版本号（如 Python 3.10.0），说明安装成功 ✅

#### 第3步：安装依赖包

1. **打开命令提示符**（Win+R → 输入 `cmd` → 回车）
2. **进入项目目录**（假设您解压到 D 盘）：
   ```bash
   D:
   cd D:\network-detector
   ```
3. **创建虚拟环境**（推荐，避免冲突）：
   ```bash
   python -m venv venv
   ```
4. **激活虚拟环境**：
   ```bash
   venv\Scripts\activate
   ```
   激活后，命令行前面会出现 `(venv)` 字样 ✅

5. **安装所有依赖**：
   ```bash
   pip install -r requirements.txt
   ```
   ⏳ 等待安装完成（可能需要 5-10 分钟）

**安装成功标志**：看到 `Successfully installed` 字样

#### 第4步：测试安装

在激活的虚拟环境中，输入：
```bash
python -c "import streamlit; print('安装成功！')"
```
如果显示"安装成功！"，说明一切正常 ✅

---

## 3. 快速开始

### 🎯 新手必读：5 分钟快速上手

#### 场景一：实时监控您的网络

**目标**：像看监控一样，实时观察网络中有没有异常

**步骤**：

1. **启动程序**
   - 确保虚拟环境已激活（命令行有 `(venv)`）
   - 输入：
     ```bash
     streamlit run src/streamlit_app.py
     ```
   - 等待几秒，浏览器会自动打开一个网页

2. **选择网络接口**
   - 在网页左侧，找到"流量来源"区域
   - 选择"实时抓包"
   - 从"选择网络接口"下拉框中选择您的网卡
     - **Windows 用户**：通常选"Wi-Fi"（无线）或"以太网"（有线）
     - 如果下拉框是空的，直接输入 `Wi-Fi` 或 `以太网`

3. **开始监控**
   - 点击"开始抓包"按钮
   - ✅ 看到"正在抓包..."的提示
   - 观察右侧的图表开始变化

4. **看懂监控界面**

   **实时仪表盘**（最上方）：
   - 📦 已捕获数据包：已经收到的网络数据包数量
   - ⚡ 最近处理数据包：最近处理的数量
   - 🟢 抓包状态：显示"运行中"
   - 🌐 网络接口：您选择的网卡

   **流量分析图表**（中间）：
   - 📈 流量趋势：折线图显示流量变化
   - 🥧 协议分布：饼图显示各种网络协议的占比
   - 📋 Top-N 流量：显示流量最大的 10 个连接

   **实时告警**（右侧）：
   - 如果检测到异常，会在这里显示
   - 点击告警可以查看详情

5. **停止监控**
   - 点击左侧的"停止抓包"按钮
   - 数据会保存到 `data/alerts.log` 文件中

#### 场景二：分析网络数据文件

**目标**：分析一个已经保存的网络数据文件（pcap 格式）

**步骤**：

1. **准备 pcap 文件**
   - 如果您没有，可以从网上下载示例文件
   - 或者先用实时抓包保存：点击"停止抓包"后，数据会自动保存

2. **上传文件**
   - 在网页左侧，选择"pcap 文件"
   - 点击"选择 pcap 文件"按钮
   - 选择您的 `.pcap` 或 `.pcapng` 文件

3. **等待分析**
   - 系统会自动处理，显示进度
   - 分析完成后显示统计信息

4. **查看结果**
   - **基础统计**：总包数、分析时长、告警数量
   - **检测性能**：检测准确率等指标
   - **流量特征表格**：每个时间段的数据汇总

---

## 4. 界面详解

### 4.1 整体布局

```
┌─────────────────────────────────────────────────────────────┐
│  📊 网络异常流量检测器                    [···] [↻] [✕]   │
├─────────────┬──────────────────────────────────────────────┤
│             │  📈 实时仪表盘                               │
│  🎛️ 左侧    │  ┌────────────────────────────────────┐    │
│  控制面板   │  │ 📦 已捕获: 1,234   ⚡ 最近: 100    │    │
│             │  │ 🟢 运行中     🌐 Wi-Fi            │    │
│             │  └────────────────────────────────────┘    │
│  📁 流量来  │                                              │
│  源：       │  📊 流量分析图表                             │
│  ○ 实时抓包 │  [折线图] [饼图] [表格]                     │
│  ○ pcap文件│                                              │
│             │  🚨 实时告警                                 │
│  [网络接口] │  [告警列表]                                 │
│  [BPF过滤]  │                                              │
│             │                                              │
│  [开始]     │                                              │
│  [停止]     │                                              │
└─────────────┴──────────────────────────────────────────────┘
```

### 4.2 左侧控制面板详解

#### ① 流量来源选择
- **实时抓包**：直接从网卡捕获实时流量
- **pcap 文件**：分析已保存的文件

#### ② 网络接口选择（仅实时模式）
- **下拉框**：显示所有可用的网卡
- **手动输入**：如果下拉框为空，可直接输入网卡名
  - Windows：`Wi-Fi`、`以太网`、`本地连接`
  - Linux：`eth0`、`wlan0`、`ens33`
  - Mac：`en0`、`en1`

#### ③ BPF 过滤表达式（可选）
这是用来**过滤不需要的流量**的，就像给监控摄像头加个过滤器。

**常用过滤示例**：
| 需求 | 输入内容 | 说明 |
|------|---------|------|
| 只看网页访问 | `tcp port 80 or tcp port 443` | 只监控 80/443 端口 |
| 只看某个 IP | `host 192.168.1.1` | 只监控与 192.168.1.1 的通信 |
| 排除自己 | `not host 192.168.1.100` | 不监控自己的 IP |
| 只看 TCP | `tcp` | 只监控 TCP 协议 |
| 全部监控 | 留空 | 监控所有流量（默认） |

**新手建议**：第一次使用**不要填**，留空即可！

#### ④ 控制按钮
- **开始抓包**：启动监控
- **停止抓包**：停止监控，保存数据

### 4.3 主显示区域

#### 实时仪表盘
显示当前监控状态，包括：
- 已捕获数据包数量
- 抓包是否运行中
- 使用的网络接口

#### 流量分析图表
- **流量趋势图**：横轴是时间，纵轴是数据包数量，观察流量高峰
- **协议分布饼图**：显示 TCP、UDP、ICMP 等协议占比
- **Top-N 流量表格**：显示流量最大的连接，包括源IP、目的IP、端口、协议、包数、字节数

#### 实时告警
- 显示最近检测到的异常事件
- 点击每个告警可以展开查看详情
- 告警按时间倒序排列（最新的在最上面）

---

## 5. 常用操作

### 5.1 日常监控流程

**每天上班第一件事：**

1. 启动程序
2. 选择"实时抓包"
3. 选择网卡（通常是 Wi-Fi 或以太网）
4. 点击"开始抓包"
5. 最小化窗口，继续工作
6. 偶尔看一眼，有没有红色告警
7. 下班前点击"停止抓包"

**数据保存位置**：
- 告警日志：`data/alerts.log`
- 上传的文件：`data/uploads/`
- 数据库：`data/traffic_analyzer.db`

### 5.2 发现异常怎么办？

**步骤 1：查看告警详情**
- 在"实时告警"区域，点击告警条目
- 查看：
  - **告警类型**：什么攻击（如 SYN Flood）
  - **源 IP**：攻击者地址
  - **目的 IP**：被攻击的目标
  - **风险分数**：0-100，越高越危险
  - **详细信息**：具体数值（如 SYN 包数量）

**步骤 2：判断严重程度**
| 风险分数 | 严重程度 | 建议操作 |
|---------|---------|---------|
| 0-30 | 低风险 | 记录观察，无需立即处理 |
| 30-60 | 中风险 | 关注，检查相关设备 |
| 60-80 | 高风险 | 立即排查，可能正在被攻击 |
| 80-100 | 极高风险 | 紧急处理，考虑断网或隔离 |

**步骤 3：处理建议**
- **端口扫描**：可能是黑客在探测，检查是否有漏洞
- **SYN 洪水/DDoS**：立即联系网络运营商或安全团队
- **记录证据**：截图或导出日志，用于后续分析

### 5.3 导出数据

**导出告警日志**：
1. 打开 `data/alerts.log` 文件（用记事本即可）
2. 每行是一个 JSON 格式的告警
3. 可以复制到 Excel 或导入数据库

**导出流量特征**：
```python
# 从数据库导出（需要简单编程）
from core.database import DatabaseManager
import pandas as pd

db = DatabaseManager()
df = pd.DataFrame(db.get_historical_traffic(limit=1000))
df.to_csv("流量数据.csv", index=False, encoding='utf-8-sig')
```

### 5.4 定期维护

**每周任务**：
- [ ] 检查告警日志，确认无异常
- [ ] 清理 `data/uploads/` 中的旧文件
- [ ] 备份 `data/traffic_analyzer.db` 数据库

**每月任务**：
- [ ] 查看数据库大小，如果超过 100MB，考虑清理
- [ ] 检查系统日志，确认程序正常运行

---

## 6. 理解检测结果

### 6.1 告警类型详解

#### 🔴 SYN Flood（SYN 洪水攻击）
- **是什么**：攻击者发送大量 TCP 连接请求（SYN包），但不完成连接，耗尽服务器资源
- **如何识别**：短时间内大量 SYN 包（默认 >50 个/5秒）
- **危害**：服务器无法响应正常用户，服务瘫痪
- **应对**：
  1. 立即记录源 IP
  2. 在防火墙上封禁该 IP
  3. 联系网络管理员

#### 🔴 DDoS Attack（分布式拒绝服务攻击）
- **是什么**：多台机器同时发起攻击，流量巨大
- **如何识别**：突发流量极高，连接数暴增
- **危害**：网络带宽耗尽，整个网络瘫痪
- **应对**：
  1. 立即联系网络运营商
  2. 启用流量清洗服务
  3. 记录攻击特征，用于溯源

#### 🟡 Port Scan（端口扫描）
- **是什么**：黑客在探测您的网络开放了哪些端口，寻找漏洞
- **如何识别**：短时间内访问大量不同端口（默认 >10 个）
- **危害**：可能是攻击前兆
- **应对**：
  1. 记录扫描者 IP
  2. 检查该 IP 是否在防火墙规则中
  3. 加强访问控制

#### 🟡 Packet Flood（数据包洪水）
- **是什么**：发送海量数据包，占用网络资源
- **如何识别**：包速率异常高（默认 >1000 包/5秒）
- **危害**：网络设备过载
- **应对**：分析源 IP，必要时封禁

#### 🟢 Byte Flood（字节洪水）
- **是什么**：发送大量数据，占用带宽
- **如何识别**：字节数异常高（默认 >100000 字节/5秒）
- **危害**：带宽耗尽
- **应对**：限制该连接的带宽

### 6.2 风险分数解读

系统根据异常程度给出 0-100 的分数：

| 分数区间 | 等级 | 含义 | 建议 |
|---------|------|------|------|
| 0-20 | 正常 | 基本正常，轻微波动 | 无需处理 |
| 21-40 | 关注 | 有异常，但影响小 | 记录观察 |
| 41-60 | 警告 | 明显异常，需关注 | 检查原因 |
| 61-80 | 危险 | 严重异常，可能被攻击 | 立即处理 |
| 81-100 | 紧急 | 极严重，正在遭受攻击 | 紧急响应 |

**注意**：分数不是绝对的，需要结合业务场景判断。例如，视频会议期间流量大是正常的。

### 6.3 流量趋势图怎么看？

```
流量趋势
1000 ┤      ╱╲
 800 ┤     ╱  ╲   ╱╲
 600 ┤    ╱    ╲╱  ╲
 400 ┤   ╱          ╲
 200 ┤  ╱            ╲
   0 ┼─────────────────
      10:00 10:05 10:10
```

- **横轴**：时间（从左到右）
- **纵轴**：数据包数量（越高流量越大）
- **正常情况**：曲线平稳或有规律波动
- **异常情况**：突然出现尖峰（可能是攻击）

**典型场景**：
- **上班打卡**：9:00 左右出现高峰（正常）
- **文件传输**：短时间内大流量（正常）
- **攻击特征**：持续高位或周期性尖峰（需排查）

---

## 7. 常见问题

### ❓ 安装问题

**Q1：运行 `pip install` 时提示"权限不足"**
- **A**：以管理员身份运行命令提示符（右键 → 以管理员身份运行）

**Q2：提示"找不到 python 命令"**
- **A**：Python 没有添加到 PATH。重新安装 Python，务必勾选"Add Python to PATH"

**Q3：某个包安装失败**
- **A**：尝试使用国内镜像源：
  ```bash
  pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
  ```

**Q4：激活虚拟环境后命令行没有变化**
- **A**：确认路径正确。如果提示`'venv\Scripts\activate'不是内部或外部命令`，说明当前不在项目目录，先用 `cd` 进入项目文件夹

### ❓ 运行问题

**Q5：启动后浏览器没自动打开**
- **A**：手动打开浏览器，访问 http://localhost:8501

**Q6：提示"端口 8501 已被占用"**
- **A**：另一个程序占用了端口。解决方案：
  1. 关闭其他 Streamlit 程序
  2. 或使用其他端口：
     ```bash
     streamlit run src/streamlit_app.py --server.port 8502
     ```

**Q7：选择网络接口后点击开始没反应**
- **A**：
  1. 确认以管理员身份运行
  2. 检查网卡名称是否正确（在命令提示符输入 `ipconfig` 查看）
  3. 尝试不填 BPF 过滤，留空

**Q8：抓包后没有数据显示**
- **A**：
  1. 确认网络有流量（打开网页、视频等）
  2. 检查 BPF 过滤是否太严格（清空）
  3. 尝试其他网卡（如有）
  4. 查看命令行窗口是否有错误信息

**Q9：分析 pcap 文件时卡住或崩溃**
- **A**：
  1. 文件太大？尝试分割文件或只分析前 10000 个包
  2. 文件格式不对？确保是 `.pcap` 或 `.pcapng` 格式
  3. 内存不足？关闭其他程序，或分析小文件

### ❓ 功能问题

**Q10：为什么有些告警没有检测到？**
- **A**：
  1. 阈值设置过高，降低阈值（见"高级配置"章节）
  2. 攻击流量太小，未达到检测标准
  3. 攻击类型不在规则库中

**Q11：误报太多（报假警）**
- **A**：
  1. 调整规则阈值，提高门槛
  2. 某些正常业务（如备份、更新）会产生大流量，可在业务低峰期监控
  3. 使用白名单功能（需修改代码）

**Q12：如何确认检测是准确的？**
- **A**：
  1. 使用已知攻击工具（如 hping3、nmap）测试
  2. 对比 Wireshark 手动分析
  3. 查看数据库中的详细特征值，判断是否真的异常

### ❓ 数据问题

**Q13：数据保存在哪里？**
- **A**：
  - 告警日志：`data/alerts.log`
  - 上传文件：`data/uploads/`
  - 数据库：`data/traffic_analyzer.db`
  - 建议定期备份这些文件

**Q14：如何删除历史数据？**
- **A**：
  - 删除日志：直接删除 `data/alerts.log`
  - 清空数据库：删除 `data/traffic_analyzer.db`（会丢失所有历史）
  - 清空上传文件：删除 `data/uploads/` 中的文件

**Q15：数据库文件越来越大怎么办？**
- **A**：
  1. 定期清理旧数据（见下问）
  2. 设置自动清理脚本（需编程）
  3. 只保留最近 30 天的数据

**Q16：如何只查看某一天的告警？**
- **A**：用 Excel 或文本编辑器打开 `data/alerts.log`，按日期筛选

---

## 8. 实用技巧

### 8.1 提高监控效果

#### 技巧 1：精准过滤，减少干扰
在 BPF 过滤框输入：
```
not host 192.168.1.1 and not host 192.168.1.2
```
这样就不会监控内部服务器，专注于外部访问。

#### 技巧 2：分时段监控
- **上班时间（9:00-18:00）**：关注业务流量
- **夜间（0:00-6:00）**：异常流量更明显，加强监控

#### 技巧 3：多网卡监控
如果有多个网卡，可以：
1. 同时打开多个浏览器窗口
2. 每个窗口监控一个网卡
3. 对比不同网卡的流量

### 8.2 快速定位问题

**场景：突然告警，说检测到 DDoS**

1. **立即查看告警详情**
   - 源 IP 是哪里？（内部还是外部）
   - 目的 IP 是什么？（是服务器还是普通电脑）
   - 风险分数多高？

2. **快速验证**
   - 打开任务管理器，看网络使用率
   - 打开 Wireshark（如有），抓包确认
   - Ping 一下目的 IP，看是否可达

3. **临时处置**
   - 如果是内部中毒：隔离该电脑
   - 如果是外部攻击：防火墙封禁源 IP
   - 如果无法处理：立即上报

### 8.3 与现有系统配合

#### 配合 Wireshark
1. 本系统发现异常 IP
2. 在 Wireshark 中使用过滤器：`host 192.168.1.100`
3. 深入分析该 IP 的详细流量

#### 配合防火墙
1. 从告警中提取攻击 IP
2. 在 Windows 防火墙中添加规则：
   ```
   入站规则 → 新建规则 → 自定义 → 远程 IP 地址 → 添加攻击 IP → 阻止
   ```

#### 配合监控系统
将告警日志导入 Zabbix、Prometheus 等监控平台：
```bash
# 定时读取最新告警
tail -f data/alerts.log | while read line; do
    # 发送到监控系统
    curl -X POST -d "$line" http://监控系统地址/api/alerts
done
```

### 8.4 定期检查清单

**每日检查**：
- [ ] 打开程序，确认在运行
- [ ] 查看实时告警，无红色紧急告警
- [ ] 流量趋势图正常（无异常尖峰）

**每周检查**：
- [ ] 导出本周告警，统计告警类型
- [ ] 查看 Top-N 流量，确认无异常主机
- [ ] 清理 `data/uploads/` 中的临时文件

**每月检查**：
- [ ] 备份数据库文件
- [ ] 检查磁盘空间
- [ ] 更新规则配置（如有新威胁）
- [ ] 评估阈值是否需要调整

---

## 9. 进阶功能（可选）

### 9.1 自定义检测规则

**场景**：您发现某种特定攻击模式，想增加检测规则

**步骤**：

1. 创建规则文件 `custom_rules.json`：
   ```json
   {
     "my_custom_rule": {
       "threshold": 100,
       "description": "我的自定义规则",
       "weight": 0.8
     }
   }
   ```

2. 修改代码（需简单编程）：
   在 `src/core/detection_engine.py` 的 `RuleMatcher` 类中，添加检测逻辑。

3. 重启程序，新规则生效

**提示**：如果不熟悉编程，可以联系开发人员协助

### 9.2 调整检测灵敏度

**降低误报**（提高阈值）：
```python
# 在 src/core/detector.py 中修改
self.rules = {
    "syn_flood": {"threshold": 100, ...},  # 从 50 改为 100
    "packet_flood": {"threshold": 2000, ...},  # 从 1000 改为 2000
}
```

**提高检测能力**（降低阈值）：
```python
self.rules = {
    "syn_flood": {"threshold": 30, ...},  # 更敏感
}
```

**修改后重启程序即可生效**

### 9.3 使用机器学习模型（高级）

如果您有标注好的攻击数据，可以训练自己的模型：

1. 准备训练数据 CSV 文件，包含特征和标签
2. 运行训练脚本：
   ```python
   from core.detection_engine import MLModel
   import pandas as pd
   
   df = pd.read_csv("训练数据.csv")
   X = df.drop("is_anomaly", axis=1)
   y = df["is_anomaly"]
   
   model = MLModel(model_type="random_forest")
   model.train(X, y)
   model.save_model("models/my_model.pkl")
   ```
3. 修改 `src/streamlit_app.py`，加载自定义模型

**注意**：此功能需要一定的编程基础和数据科学知识

---

## 10. 获取帮助

### 10.1 遇到问题怎么办？

**第一步：查看本手册**
- 阅读"常见问题"章节
- 使用浏览器搜索（Ctrl+F）查找关键词

**第二步：查看日志**
- 命令行窗口会显示错误信息
- 查看 `data/alerts.log` 了解程序运行情况

**第三步：搜索解决方案**
- 将错误信息复制到搜索引擎
- 加上关键词"streamlit"、"scapy"

**第四步：联系支持**
- 项目 GitHub：https://github.com/yourusername/network-anomaly-detector
- 提交 Issue，描述清楚：
  1. 操作系统和版本
  2. 错误信息（截图）
  3. 复现步骤
  4. 期望结果

### 10.2 学习资源推荐

**网络基础**：
- 《计算机网络：自顶向下方法》
- 中国大学 MOOC 相关课程

**Python 入门**：
- 廖雪峰 Python 教程（免费在线）
- 菜鸟教程 Python3 教程

**网络安全**：
- CTF 比赛平台（练习攻防）
- 网络安全工程师认证（如 CISSP、CISP）

---

## 11. 附录

### 11.1 快捷键

**Streamlit 界面**：
- `Ctrl+R` 或 `F5`：刷新页面
- `Ctrl+F`：在页面中搜索

**命令提示符**：
- `Ctrl+C`：停止程序
- `↑` `↓`：查看历史命令
- `Tab`：自动补全文件名

### 11.2 推荐工具

**网络分析**：
- Wireshark：专业的网络抓包分析工具
- Nmap：网络扫描工具

**系统监控**：
- 任务管理器（Windows）：查看资源使用
- top/htop（Linux）：查看进程状态

**文本编辑**：
- VS Code：代码编辑，查看日志
- Notepad++：查看大文件

### 11.3 术语表

| 术语 | 通俗解释 |
|------|---------|
| **数据包** | 网络通信的基本单位，就像一封信 |
| **IP 地址** | 网络设备的"门牌号"，如 192.168.1.1 |
| **端口** | 同一台电脑上不同程序的"信箱号"，如 80 是网页 |
| **协议** | 通信的"语言"，如 TCP、UDP |
| **抓包** | 把网络上传输的数据包"抓"下来分析 |
| **BPF 过滤** | 只抓取符合条件的包，像用筛子过滤 |
| **特征** | 数据包的"指纹"，用来判断是否异常 |
| **阈值** | 触发告警的"分数线" |
| **误报** | 把正常流量当成异常（报假警） |
| **漏报** | 没发现真正的异常 |

---

## 12. 版本更新说明

### v1.0.0 (2026-02-14)
- ✅ 首次发布
- ✅ 实时抓包和离线分析功能
- ✅ 10 种攻击检测规则
- ✅ 可视化仪表盘
- ✅ SQLite 数据持久化

---

## 📞 联系我们

- **项目主页**：https://github.com/yourusername/network-anomaly-detector
- **问题反馈**：GitHub Issues
- **邮箱**：your.email@example.com

---

## 🎓 结语

恭喜您完成了本手册的学习！现在您已经掌握了：

✅ 如何安装和启动系统  
✅ 如何进行实时监控和文件分析  
✅ 如何理解检测结果  
✅ 如何处理常见异常  
✅ 如何优化监控效果

**记住**：
1. 第一次使用建议在测试环境练习
2. 不要盲目调整阈值，先观察再优化
3. 发现严重攻击及时上报
4. 定期备份重要数据

祝您使用愉快！🚀

---

**最后更新：2026年2月14日**