# 网络异常流量检测系统 - 启动指南

本指南帮助您快速启动系统的三种前端界面，适用于评委演示、开发调试和离线部署场景。

---

## 环境要求

### 必需环境

- **Python**：3.10 或更高版本
- **操作系统**：Windows 10/11、macOS 10.15+、Linux（Ubuntu 20.04+）

### Windows 特别要求

实时抓包功能需要安装 **Npcap**（WinPcap 的现代替代品）：

1. 访问 [Npcap 官网](https://npcap.com/#download) 下载安装包
2. 安装时勾选 **"Install Npcap in WinPcap API-compatible Mode"**
3. 重启计算机使驱动生效

### 权限要求

**实时抓包**需要管理员/root 权限：

- **Windows**：右键点击终端，选择"以管理员身份运行"
- **Linux/macOS**：使用 `sudo` 运行命令

**PCAP 文件分析**无需特殊权限。

---

## 安装依赖

在项目根目录执行：

```bash
pip install -r requirements.txt
```

**依赖安装时间**：约 2-5 分钟（取决于网络速度）

**常见问题**：
- 如遇到 PyTorch 安装缓慢，可使用清华镜像源：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`
- 如提示权限错误，添加 `--user` 参数：`pip install -r requirements.txt --user`

---

## 启动方式（三选一）

### 方式 1：Dash Web UI（推荐）

**适用场景**：评委演示、功能展示、日常使用

**启动命令**：

```bash
python DashWeb.py
```

**访问地址**：`http://localhost:8050`

**特点**：
- 现代化 Web 界面，基于 Dash + Plotly
- 四大功能页面：概览、实时监控、PCAP 分析、历史记录
- 响应式布局，支持移动端访问
- 交互式图表，支持缩放、筛选、导出

**启动成功标志**：

```
Dash is running on http://127.0.0.1:8050/

 * Serving Flask app 'app'
 * Debug mode: off
WARNING: This is a development server. Do not use it in a production deployment.
 Press CTRL+C to quit
```

---

### 方式 2：Streamlit UI（备用）

**适用场景**：快速原型、轻量级演示

**启动命令**：

```bash
streamlit run src/streamlit_app.py
```

**访问地址**：`http://localhost:8501`

**特点**：
- 简洁的单页应用
- 左侧边栏切换模式（实时抓包/PCAP 分析）
- 自动刷新，实时更新数据
- 适合快速验证功能

**启动成功标志**：

```
You can now view your Streamlit app in your browser.

  Local URL: http://localhost:8501
  Network URL: http://192.168.x.x:8501
```

---

### 方式 3：PyQt5 桌面客户端

**适用场景**：离线演示、无浏览器环境、独立部署

**启动命令**：

```bash
python desktop_app.py
```

**特点**：
- 原生桌面应用，无需浏览器
- 侧边栏导航：概览、实时监控、PCAP 分析、告警中心
- 独立窗口，支持最小化到托盘
- 适合打包成 exe 分发

**启动成功标志**：

直接弹出应用窗口，标题栏显示 **"Scapy Sentinel · 网络异常流量检测器"**

---

## 使用流程

### 实时监控流程

1. **选择启动方式**（推荐 Dash Web UI）
2. **进入实时监控页面**
3. **选择网络接口**：
   - Windows：通常是 `以太网` 或 `WLAN`
   - Linux：通常是 `eth0` 或 `wlan0`
   - macOS：通常是 `en0`
4. **点击"开始监控"**（需管理员权限）
5. **查看实时数据**：
   - 流量趋势图（包速率、字节速率）
   - 协议分布饼图
   - 告警列表（按严重程度排序）
6. **停止监控**：点击"停止"按钮

### PCAP 分析流程

1. **进入 PCAP 分析页面**
2. **上传文件**：
   - 支持格式：`.pcap`、`.pcapng`
   - 文件大小：建议 < 500MB（大文件分析较慢）
3. **等待分析**：
   - 进度条显示解析进度
   - 分析时间取决于文件大小（1MB 约 2-5 秒）
4. **查看结果**：
   - 流量统计摘要
   - 协议分布
   - 告警列表（攻击类型、置信度、触发规则）
5. **导出报告**：点击"生成报告"按钮，下载 Markdown 格式分析报告

---

## 常见问题

### Q1: 启动时提示"找不到网络接口"

**原因**：未安装 Npcap（Windows）或 libpcap（Linux/macOS）

**解决方案**：
- **Windows**：安装 [Npcap](https://npcap.com/#download)
- **Linux**：`sudo apt-get install libpcap-dev`（Debian/Ubuntu）或 `sudo yum install libpcap-devel`（CentOS/RHEL）
- **macOS**：libpcap 已预装，无需额外安装

---

### Q2: 实时抓包提示"权限不足"

**原因**：未以管理员/root 权限运行

**解决方案**：
- **Windows**：右键点击 PowerShell/CMD，选择"以管理员身份运行"，然后执行启动命令
- **Linux/macOS**：在命令前加 `sudo`，例如 `sudo python DashWeb.py`

---

### Q3: Transformer 模型加载失败

**原因**：模型文件缺失或 PyTorch 版本不兼容

**解决方案**：
1. 确认 `models/transformer_cicids.pth` 文件存在
2. 检查 PyTorch 版本：`pip show torch`（需 >= 1.10）
3. 如文件缺失，从项目仓库重新下载或使用训练脚本重新训练：
   ```bash
   python scripts/train_with_cicids.py
   ```

---

### Q4: 网页无法访问（ERR_CONNECTION_REFUSED）

**原因**：端口被占用或防火墙拦截

**解决方案**：
1. **检查端口占用**：
   - Windows：`netstat -ano | findstr :8050`
   - Linux/macOS：`lsof -i :8050`
2. **更换端口**：
   - Dash：编辑 `DashWeb.py`，修改 `app.run_server(port=8050)` 为其他端口
   - Streamlit：使用 `streamlit run src/streamlit_app.py --server.port 8502`
3. **关闭防火墙**（临时）：
   - Windows：控制面板 → Windows Defender 防火墙 → 关闭
   - Linux：`sudo ufw disable`

---

### Q5: 抓包时看不到任何流量

**原因**：选择了错误的网络接口或接口未激活

**解决方案**：
1. **确认接口状态**：
   - Windows：`ipconfig`，查看"以太网适配器"或"无线局域网适配器"
   - Linux/macOS：`ifconfig` 或 `ip addr`，确认接口有 IP 地址
2. **尝试其他接口**：在下拉菜单中切换不同的网络接口
3. **生成测试流量**：打开浏览器访问网站，或使用 `ping 8.8.8.8` 生成 ICMP 流量

---

### Q6: 依赖安装时出现编译错误

**原因**：缺少 C/C++ 编译工具链

**解决方案**：
- **Windows**：安装 [Microsoft C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)
- **Linux**：`sudo apt-get install build-essential python3-dev`
- **macOS**：`xcode-select --install`

---

## 性能优化建议

### 实时抓包优化

1. **限制抓包速率**：在代码中设置 `count` 参数，例如每秒最多抓 1000 个包
2. **使用 BPF 过滤器**：只抓取关心的流量，例如 `tcp port 80 or tcp port 443`
3. **减少时间窗口**：将特征聚合窗口从 5 秒改为 3 秒，降低内存占用

### PCAP 分析优化

1. **分批处理**：大文件（> 500MB）建议先用 Wireshark 拆分成多个小文件
2. **关闭 Transformer**：如只需快速检测，可在代码中禁用 Transformer 检测器
3. **增加内存**：分析大文件时，确保系统可用内存 > 4GB

---

## 下一步

- **查看用户手册**：`用户手册.md` - 详细功能说明与操作指南
- **桌面客户端专项说明**：`桌面客户端使用说明.md` - PyQt5 客户端详细使用方法
- **技术文档**：`docs/` 目录 - 架构设计、模型训练、部署说明

---

## 技术支持

如遇到其他问题，请检查：

1. **Python 版本**：`python --version`（需 >= 3.10）
2. **依赖版本**：`pip list`，对比 `requirements.txt`
3. **系统日志**：查看终端输出的错误信息
4. **项目文档**：`README.md` - 系统架构与技术栈说明

---

**祝您使用愉快！**
