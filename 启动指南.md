# 网络异常流量检测器启动指南

## 项目概述
这是一个基于Scapy和Streamlit的网络异常流量检测器，集成了规则检测、机器学习和Transformer模型的混合检测引擎。

## 系统要求
- Python 3.8+
- Windows/macOS/Linux
- 可选：CUDA环境（用于加速Transformer模型推理）

## 启动步骤

### 1. 安装依赖

```bash
# 在项目根目录执行
pip install -r requirements.txt
```

### 2. 初始化Transformer模型

项目已包含训练好的Transformer模型文件 `models/transformer_cicids.pth`，无需额外训练即可使用。

### 3. 启动Streamlit应用

```bash
# 在项目根目录执行
cd src
streamlit run streamlit_app.py
```

或者使用完整路径：

```bash
streamlit run src/streamlit_app.py
```

### 4. 访问应用

启动成功后，会自动打开浏览器，访问地址通常为：
`http://localhost:8501`

## 使用方法

### 实时抓包模式
1. 在左侧选择"实时抓包"模式
2. 选择网络接口（如Wi-Fi或以太网）
3. 可选：设置BPF过滤表达式（默认为"tcp or udp"）
4. 点击"开始抓包"按钮
5. 查看实时仪表盘、流量分析图表和告警信息

### PCAP文件分析模式
1. 在左侧选择"pcap文件"模式
2. 上传PCAP或PCAPNG格式的网络数据包文件
3. 等待系统解析和分析
4. 查看流量统计、特征视图和异常告警

### 历史数据查询
在应用底部可以查询历史告警、历史流量特征和数据库统计信息。

## 功能说明

### 核心模块
- **Source**：网络流量来源（实时抓包或PCAP文件）
- **Parser**：数据包解析模块
- **Feature Extractor**：特征提取与时间窗口聚合
- **Detection Engine**：混合检测引擎（规则+机器学习+Transformer）
- **Sink**：告警记录与可视化

### 检测能力
- 基于规则的快速检测（SYN洪水、端口扫描等）
- 基于机器学习的异常检测
- 基于Transformer的时序异常检测

## 注意事项

1. 实时抓包模式需要管理员/root权限
2. 确保防火墙设置允许应用访问网络接口
3. 首次使用时，数据库会自动创建
4. 长时间运行时，建议定期清理数据目录

## 常见问题

### Q: 实时抓包没有显示数据？
A: 确保选择了正确的网络接口，并且具有足够的权限。

### Q: Transformer模型没有生效？
A: 确保模型文件 `models/transformer_cicids.pth` 存在，并且检测引擎已正确加载。

### Q: 应用运行缓慢？
A: 尝试减少捕获的数据包数量，或调整时间窗口大小。

## 技术支持
如有其他问题，请查看项目文档或联系开发人员。